This is an HTML5 canvas-based timer for midterm and final exams.

It was written with a general-purpose animation loop, so other 
animations could be added.

It was written in CoffeeScript, and compiled to JavaScript. So the
.js JavaScript file might not be very readable.


